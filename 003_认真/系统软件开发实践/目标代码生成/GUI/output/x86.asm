DATA SEGMENT

DATA ENDS

CODE SEGMENT
	ASSUME CS: CODE, DS: DATA

START:
	MOV AX, DATA
	MOV DS, AX

ALLOC:
	MOV AL, 0
	MOV [1], AL
	MOV AL, 0
	MOV [2], AL
	MOV AL, 0
	MOV [3], AL

MAIN:
	MOV AL, 12
	MOV [1], AL
	MOV AL, 13
	MOV [2], AL
	MOV AL, [1]
	MOV AH, AL
	MOV AL, [2]
	MOV BL, AL
	ADD AH, BL
	MOV AL, AH
	PUSH AX
	MOV BL, 2
	MOV AL, AL
	MOV AH, BL
	MUL AH
	MOV BH, AL
	POP AX
	PUSH AX
	MOV BL, 3
	MOV AL, BH
	MOV AH, 0
	DIV BL
	MOV [3], AL
	POP AX

CODE ENDS
END START
